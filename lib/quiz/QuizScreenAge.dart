import 'package:flutter/material.dart';

class QuizScreenAge extends StatelessWidget {
  const QuizScreenAge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink[300],
    );
  }
}
