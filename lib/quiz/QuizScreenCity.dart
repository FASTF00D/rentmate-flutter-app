import 'package:flutter/material.dart';

class QuizScreenCity extends StatelessWidget {
  const QuizScreenCity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey,
    );
  }
}
