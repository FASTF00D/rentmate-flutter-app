import 'package:flutter/material.dart';

class QuizScreenJob extends StatelessWidget {
  const QuizScreenJob({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
